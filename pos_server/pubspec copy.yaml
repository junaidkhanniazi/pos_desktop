name: pos_server
description: POS Desktop Server for Flutter Desktop App
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  mysql1: ^0.20.0
  logger: ^2.6.2

dev_dependencies:
  test: ^1.21.0
