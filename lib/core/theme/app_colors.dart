import 'package:flutter/material.dart';

class AppColors {
  // Core Brand Palette
  static const Color primary = Color(0xFF5667FF);
  static const Color secondary = Color(0xFF26C6DA);
  static const Color accent = Color(0xFFFF7043);

  // Neutrals
  static const Color background = Color(0xFFF7F9FC);
  static const Color surface = Colors.white;
  static const Color border = Color(0xFFE0E6ED);

  // Text
  static const Color textDark = Color(0xFF1C1C28);
  static const Color textMedium = Color(0xFF5A5A89);
  static const Color textLight = Color(0xFF9FA8DA);

  // States
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFB300);
  static const Color error = Color(0xFFD32F2F);

  // Shadows
  static const Color shadow = Color(0x1A000000);
}
